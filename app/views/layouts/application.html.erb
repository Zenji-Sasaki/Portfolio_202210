<!DOCTYPE html>
<html>
  <head>
    <title>EssRank</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom shadow-sm">
      <h5 class="my-0 mr-md-auto font-weight-normal"><%= link_to "EssRank", root_path %></h5>
      <nav class="my-2 my-md-0 mr-md-3">
        <a class="p-2 text-dark"><%= link_to "レストランから探す", restaurants_path %></a>
        <a class="p-2 text-dark"><%= link_to "商品から探す", products_path %></a>
        <% if user_signed_in? %>
        <a class="p-2 text-dark">ユーザー情報</a>
        <% end %>
      </nav>
      <% if user_signed_in? %>
      <div class="btn btn-outline-primary"><%= link_to "ログアウト", destroy_user_session_path, method: :delete %></div>
      <% else %>
      <div class="btn btn-outline-primary"><%= link_to "ログイン", new_user_session_path %></div>
      <% end %>
    </div>
    <%= yield %>
  </body>
</html>
